# Before starting application perform following steps:
1)Open command line
2)Open project folder with cd command
3)Type "dotnet ef database update --context AppIdentityDbContext" to initialise identity database
